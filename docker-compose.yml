services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: gam_auth
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build:
      context: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg2://postgres:postgres@db:5432/gam_auth
      JWT_SECRET_KEY: super-secret-key
      ACCESS_TOKEN_EXPIRE_MINUTES: 15
      REFRESH_TOKEN_EXPIRE_DAYS: 7
    ports:
      - "8000:8000"
    depends_on:
      - db
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: gam_conversao
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg2://postgres:postgres@db:5432/gam_conversao
      JWT_SECRET_KEY: super-secret-key
      ADMIN_EMAIL: admin@gamconversao.com
      ADMIN_PASSWORD: Admin123!
    depends_on:
      - db
    ports:
      - "8000:8000"

volumes:
  postgres_data:
